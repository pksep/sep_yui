import { SliderMediaType } from '../interface/interface';

export declare const isSliderImage: (path: string | null | undefined, mediaType?: SliderMediaType) => boolean;
export declare const isSliderVideo: (path: string | null | undefined, mediaType?: SliderMediaType) => boolean;
